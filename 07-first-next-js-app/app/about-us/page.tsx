// The file name is "page.tsx", and this content will be automatically displayed whent he user switches to ../about-us in the URL.
// We can not keep any other name for this file, it must be page.tsx

// Since we have 1 more folder here,i.e. "intro", so this will act as a nested route.
// Note -  In both the folders, we have a page.tsx file which wil show the relevent content.

function AboutUs() {
  return <div>About Us</div>;
}

export default AboutUs;
